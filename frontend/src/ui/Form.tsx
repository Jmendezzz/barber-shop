import styled from 'styled-components';

const Form = styled.form`
  overflow: hidden;
  font-size: 1.4rem;
  padding: 1rem 1.2rem;
  display:flex;
  flex-direction: column;
  gap: 1.3rem;

`;

export default Form;
